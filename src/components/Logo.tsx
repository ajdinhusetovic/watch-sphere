import "../scss/logo.scss";

const Logo = () => {
  return (
    <div className="logo-wrapper">
      <span id="watch">WATCH</span>
      <span id="sphere">sphere</span>
    </div>
  );
};

export default Logo;
